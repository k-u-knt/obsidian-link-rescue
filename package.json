{
  "name": "obsidian-link-rescue",
  "version": "0.1.0",
  "description": "Repairs links that don't resolve only because of an invisible or lookalike character, such as the narrow no-break space in macOS screenshot names, and shows the iCloud download state of embedded files on macOS.",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "links",
    "icloud"
  ],
  "author": "Kenta Ninomiya",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "tslib": "^2.8.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "monkey-around": "^3.0.0"
  }
}
